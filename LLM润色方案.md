# LLM 中文润色方案 · DeepSeek V4

## 目标

在不增加抓取材料之外事实的前提下，将所有非热搜新闻条目改写为中文正文。即使原始材料缺失或模型暂时不可用，展示层也必须保留不虚构的中文说明，不再回退为英文原文摘要。

## 流水线位置

```text
fetch_sources.py → build_report.py → llm_polish.py → send_feishu.py → cleanup.py
```

`llm_polish.py` 只处理新闻正文；标题和微博热搜话题保持抓取结果。新闻正文的处理顺序如下：

```text
材料补抓结果
  ↓
DeepSeek V4 Flash 首次中文润色
  ↓ 不通过事实、中文或格式校验
DeepSeek V4 Pro 修复，最多两次
  ↓ 仍失败或材料不足
不虚构事实的中文安全兜底
```

## DeepSeek 配置

| 配置项 | 作用 | 默认值 |
| --- | --- | --- |
| `DEEPSEEK_API_KEY` | API 密钥，仅保存在 `.env` 或环境变量 | 无 |
| `DEEPSEEK_PRIMARY_MODEL` | 首次批量润色模型 | `deepseek-v4-flash` |
| `DEEPSEEK_REPAIR_MODEL` | 校验失败后的修复模型 | `deepseek-v4-pro` |

接口使用 `https://api.deepseek.com/chat/completions`。两个模型均显式关闭思考模式，以获得更快、更稳定的文本改写结果；首次调用使用 Flash，只有失败条目才调用 Pro。

密钥、Webhook 和恢复码均不得写入代码、Git、日志或测试输出。`.env` 必须保持为仅运行账户可读写。

## 中文与事实校验

每一稿都必须通过以下检查：

1. 正文长度为 20～260 个字符。
2. 至少包含 10 个中文字符；英文产品名、项目名和专有名词可以保留。
3. 不包含问号、无来源引述词、标题、列表或 Markdown。
4. 输出中的数字必须能在新闻标题或抓取材料中找到；金额、日期、百分比和带千位分隔符的数字会被统一比较。
5. 只使用该条新闻的标题、摘要、热度等材料，不跨条引用信息。

校验失败不会直接采用原始英文摘要，而会将失败原因连同上一稿交给 Pro 重写。若所有调用均失败，使用固定中文说明：“该新闻的原始材料不足或润色服务暂不可用，暂未生成完整摘要，请通过原文链接查看详情。”

## 运行与观测

运行后日志会记录三类结果：

- Flash 成功条数。
- Pro 修复成功条数。
- 中文安全兜底条数及原因。

正常情况下所有非热搜新闻条目之和应等于这三类结果之和。任何数量的 API 失败都不会导致早报出现英文正文或空正文，但兜底数量上升应作为运维告警信号。

## 测试要求

本项目的单元测试覆盖：

- 英文首稿被 Pro 修复为中文。
- API 连续失败时使用中文安全兜底。
- 缺少 API 密钥时不发起请求且保持中文输出。
- 数字校验阻止新增数字。
- V4 请求使用 Flash 模型并关闭思考模式。

修改模型、Prompt 或校验逻辑后，应先运行单元测试；更换 API 密钥后，再使用不含真实新闻的一条短材料进行真实 API 冒烟验证。
