# 项目协作要求

所有参与本项目的 AI 在完成任何改动时，必须遵守以下要求：

1. **提交版本记录**：任何代码、配置、文档或其他项目文件改动完成后，必须创建 Git commit，使改动可被追踪和回滚。
2. **先检查与测试**：任何改动都必须经过与其风险相称的检查和测试；检查与测试应尽可能精简、高效。只有检查与测试通过后，改动才有效并可以创建 Git commit。若检查或测试未通过，必须继续修正；若仍无法完成检查与测试，必须向用户报告原因，不得将未验证的改动提交为完成状态。
