# 飞书接入指南 · 让早报到达你的飞书

> 原理：服务器脚本通过 HTTP POST 把早报推送到**飞书群机器人 Webhook**，群内即可收到消息。
> 不需要飞书开放平台、不需要 WorkBuddy 连接器、不需要写 App。全程 5 分钟。

---

## 一、操作步骤（飞书 App / PC 端）

1. **建群**：飞书 → 新建群聊。推荐建一个只有自己的群，命名如「早报接收」（不用拉人）。
2. **添加机器人**：进入群设置 → **群机器人** → **添加机器人** → 选择「**自定义机器人**」。
3. **机器人名称**：填 `每日新闻早报`（任意）。
4. **安全设置（关键，选一项）**：
   - ✅ **推荐「自定义关键词」** → 关键词填 `早报`
     - 原理：消息正文里必须包含该关键词才能发出。我们的早报标题是「每日新闻早报 · 2026-08-02」，天然命中 `早报`，**不用改任何代码**。
   - ❌ 「加签」：需要改 send_feishu.py 加签名头，麻烦，不选。
   - ❌ 「IP 白名单」：只适合固定 IP 场景，以后换服务器会坑，不选。
5. **完成**：创建后复制 **Webhook 地址**，形如：
   ```
   https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
   ```

## 二、配置到服务器（1 条命令）

SSH 登录服务器，把 webhook 写入 `.env`（脚本会自动读取，crontab 无需额外配置）：

```bash
echo 'FEISHU_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx' > /root/newsdigest/.env
```

## 三、测试发送

```bash
cd /root/newsdigest && .venv/bin/python send_feishu.py
```

看到 `[ok] 已发送 N 条消息到飞书`，群里出现早报即成功。

## 四、验证清单

- [ ] 群聊里出现早报内容（含标题、简述、来源）
- [ ] 服务器 `run.log` 无报错
- [ ] （第二天 08:00）crontab 自动执行，无需任何人工操作

## 五、常见问题

| 现象 | 原因与处理 |
| --- | --- |
| 飞书返回 `code=19001` 或关键词错误 | 安全设置选的是关键词但正文没命中 → 确认关键词填的是 `早报` 且发送的是当天早报 |
| 飞书返回 `code=19021` | 你选了「加签」但代码没签名 → 重新建机器人改选「自定义关键词」 |
| 返回 `code=9499` 或机器人被禁 | 群主/管理员把机器人移除了 → 群里重新添加 |
| webhook 失效 | 删除了机器人 → 重新添加拿新地址，更新 `.env` |
| 消息里能看到 `#` `**` 等符号 | 当前以纯文本发送，Markdown 符号原样显示（可读但不够美观）。**需要卡片样式随时说**，升级 send_feishu.py 用 interactive 卡片即可 |

## 六、安全说明

- Webhook 相当于群的一个"投递口"，任何拿到该地址的人都能往群里发消息。**请勿公开、勿提交到代码仓库**（已存在 `.env` 中，且 `.env` 不在打包清单内）。
- 机器人只负责接收推送，无法读取你飞书里的其他数据，权限最小。
