# Newsday 后续阶段任务

> 目标：完成 `WEBSITE_DESIGN.md` v0.2 的首版验收，并具备可重复部署和可追溯运维能力。
>
> 规则：每个阶段完成前，必须完成对应测试、更新本文档中的勾选状态，并创建 Git commit。

## 阶段 0：安全处置与可重复开发环境（上线阻塞）

- [ ] **立即轮换已暴露的凭据**：DeepSeek API Key 与飞书 Webhook 均需在服务端控制台中废止并重新生成；不得将新值发送到聊天记录或写入受版本控制文件。
- [ ] 检查本机、服务器、备份和 Git 历史中是否存在真实密钥或 Webhook；保留仅含占位符的 [`.env.example`](.env.example)。
- [ ] 用 Python 3.12 重建本地虚拟环境，并验证 `python --version`、`pip check` 与依赖安装均正常。
- [ ] 为 Python 依赖生成并提交锁定文件（例如 `uv.lock` 或带 hash 的 requirements），使部署不再依赖浮动版本范围。
- [ ] 增加统一的测试入口（例如 `Makefile` 或脚本），包含测试、静态检查和基础语法检查。
- [ ] 运行完整测试套件，修复全部失败项；将 52 个现有测试全部跑通作为完成门槛。

**验收**：全量测试通过；新环境可依据文档从零安装；工作区和 Git 历史中没有有效生产凭据。

## 阶段 1：补齐订阅生命周期（首版验收阻塞）

- [x] 实现用户主动取消订阅：停止未来任务、禁用订阅，并在界面中提供明确的确认操作。
- [x] 实现取消时删除已保存的平台凭据的选项；删除后不得再被任何 worker 解密或使用。
- [x] 在控制台展示订阅状态、分类、发送时间、平台脱敏地址、验证时间和最近 7 天任务状态。
- [x] 调整 Webhook 向导为“填写 → 测试成功 → 加密保存并验证”；测试失败时不得启用可投递目标。
- [x] 明确未验证目标的订阅状态：订阅配置可保存，但不得显示为“投递已启用”。
- [x] 增加已登录用户修改密码入口，并确保修改密码后撤销旧会话。
- [x] 为取消、删除凭据、修改密码、验证失败等关键路径补充集成测试。

**验收**：用户可完整创建、修改、暂停、恢复、取消订阅；未通过测试的 Webhook 永远不会进入调度。

## 阶段 2：投递可靠性与幂等性加固（首版验收阻塞）

- [ ] 实现超时 `sending` 任务回收：为锁定义超时阈值，将未完成任务安全转回可重试状态。
- [ ] 明确并实现重试语义：临时失败最多 3 次指数退避重试；配置类 4xx 直接失败；记录安全错误码。
- [ ] 按设计补齐 `delivery_jobs(status, scheduled_for)`、`delivery_jobs(subscription_id, scheduled_for)` 和 `delivery_attempts(delivery_job_id)` 索引及 Alembic 迁移。
- [ ] 防止同一用户同日多次投递重复新闻：选取时排除其较早已发送任务中的条目，并在新闻不足时明确提示。
- [ ] 对暂停、取消及修改订阅后的待发送任务定义并实现一致规则：新配置仅影响未来、未领取任务；暂停/取消不再创建新任务。
- [ ] 为多 worker 抢占、worker 在发送前/后崩溃、重试、锁回收、跨时段去重建立自动化测试。

**验收**：重复扫描、多 worker 和 worker 重启均不造成重复投递；卡住任务可恢复；同一用户多次投递优先获得新内容。

## 阶段 3：新闻池质量与内容规则

- [ ] 将现有抓取结果的评分规则接入新闻入池流程，而不是全部以默认分数 `0` 排序。
- [ ] 完成跨来源去重：canonical URL 去重之外，增加可审阅的标题相似度键与误杀测试。
- [ ] 补充标签生成规则，并让 AI、科技、消费电子、GitHub 之间满足设计文档规定的排他分类关系。
- [ ] 为财经、时政增加来源可信度/多源核验状态；为社会热搜保存可信状态，并在公开页和投递内容中展示“热搜不等于事实确认”。
- [ ] 审核所有启用来源的可达性、授权/使用限制和类别覆盖；失败源不得阻塞本轮入池。
- [ ] 将 DeepSeek 调用的模型、状态和 token 用量完整记录到 `news_polishes.usage_json` 或独立可清理的用量记录中。
- [ ] 补充新闻不足、材料不足、模型不可用、跨分类全局去重等内容质量测试。

**验收**：每条新闻有唯一主分类、来源和可展示的中文摘要；分类/跨分类去重可解释；高风险内容带可信提示。

## 阶段 4：后台、审计与数据保留

- [ ] 后台增加失败任务筛选、人工重试和禁用订阅/目标操作；全部管理操作写入不含敏感值的审计记录。
- [ ] 决定“预设分类管理”的首版边界：若允许后台管理，则实现受控配置与迁移；若不允许，则修订设计文档，明确为代码固定配置。
- [ ] 完成七天保留策略：清理新闻原始材料、润色内容、投递条目/正文快照；保留不含正文的投递状态和错误码。
- [ ] 为清理任务增加批处理、dry-run 输出、删除计数、失败告警及数据保留测试。
- [ ] 统一旧版文件式早报清理与新版数据库清理的职责，避免两个流程相互覆盖或遗漏。

**验收**：管理员可安全排障；七天后敏感新闻内容被清理，而必要的投递审计数据仍可查询。

## 阶段 5：部署演练与发布

- [ ] 审核并更新 Nginx：真实域名、HTTPS、反向代理头、请求大小限制和安全响应头。
- [ ] 在干净 PostgreSQL 实例验证 `alembic upgrade head`；再从前一版本备份验证升级路径。
- [ ] 完成 PostgreSQL 备份方案（每日备份、14 天保留、权限控制），取代当前仅覆盖 SQLite 的旧运维脚本。
- [ ] 将 systemd 服务、定时器、环境文件路径、运行用户和资源限制在目标服务器演练。
- [ ] 部署后验证 `/healthz`、`/readyz`、新闻入池、飞书测试发送、企业微信测试发送和一次真实定时投递。
- [ ] 完成回滚手册：数据库备份恢复、代码版本回退、密钥轮换、任务暂停和故障通知。

**验收**：从空服务器可按 [DEPLOY.md](DEPLOY.md) 完成部署；关键健康检查、测试发送和一次端到端投递均成功。

## 阶段 6：前端 Demo 处置

- [ ] 决定 [website-demo](website-demo/) 的归属：删除、独立发布为纯交互原型，或将其视觉设计迁移到 FastAPI/Jinja 正式站。
- [ ] 若保留，清理构建产物和不必要依赖，添加 README、启动方式、用途声明与独立 Git 提交。
- [ ] 不得将 Demo 的模拟“已验证/已发送”状态误作正式功能；正式站只能展示数据库中的真实状态。

**验收**：仓库中不存在用途不明、未跟踪且体积巨大的前端目录；设计稿与正式产品的边界清晰。

## 最终首版验收清单

- [ ] 游客可浏览公开新闻。
- [ ] 无有效邀请码无法创建订阅。
- [ ] 用户可管理、暂停、恢复和取消订阅。
- [ ] 10 个预设分类、5–10 条/类、单次最多 50 条均被服务端强制校验。
- [ ] 每天最多 3 个北京时间发送节点。
- [ ] 飞书和企业微信 Webhook 测试失败时不能投递。
- [ ] 任务幂等、可重试、可从异常锁定状态恢复。
- [ ] 7 天新闻内容保留策略生效。
- [ ] 密码、邀请码、恢复码、Webhook 不以明文存入数据库、日志或仓库。
- [ ] 全量自动测试、迁移演练、健康检查和真实平台端到端测试均已通过。
